import { updateUI } from "../client/js/upUI";

describe("Testing the update UI functionality", () => {
    test("Testing the updateUI() function", () => {
        expect(updateUI).toBeDefined();
    });
});